<!-- 空状态组件 -->
<view class="empty-container">
  <image class="empty-icon" src="{{icon}}" mode="aspectFit" wx:if="{{icon}}"></image>
  <text class="empty-text">{{text || '暂无数据'}}</text>
  <view class="empty-action" wx:if="{{showAction}}">
    <slot name="action"></slot>
  </view>
</view>
